# دليل تشغيل ونشر تطبيق SmartyAi Order 🚀

يرشدك هذا الدليل في عملية إعداد وتثبيت ونشر تطبيق **SmartyAi Order** للاستخدام المحلي أو للنشر السحابي بالكامل على منصات الاستضافة مثل **Render** أو **Vercel**.

---

## 1. إعداد مصادقة حسابات Google (Google Sign-In) 🔐

لضمان عمل تسجيل الدخول بـ Google بنجاح محلياً وسحابياً دون أية أخطاء، قم باتباع الخطوات التالية:

### الخطوة 1: تهيئة Google Cloud Console
1. انتقل إلى [Google Cloud Console](https://console.cloud.google.com/).
2. اختر مشروع Firebase الخاص بالتطبيق (أو أنشئ مشروعاً جديداً).
3. من القائمة الجانبية، اذهب إلى **APIs & Services** ثم **Credentials**.
4. اضغط على مفتاح الـ **OAuth 2.0 Client IDs** المفعّل لمشروعك (عادة يتم إنشاؤه تلقائياً بواسطة Firebase).

### الخطوة 2: إضافة روابط توجيه التخويل المعتمدة (Authorized Redirect URIs)
بموجب إرشادات الأمان لمصادقة Firebase، يجب عليك إضافة الروابط التالية داخل مربع **Authorized redirect URIs**:

* **للتشغيل المحلي:**
  * `http://localhost:3000`
  * `http://localhost:3000/__/auth/handler`

* **للتشغيل السحابي (استبدل `your-app` برابط استضافة Render الخاص بك):**
  * `https://your-app.onrender.com`
  * `https://your-app.onrender.com/__/auth/handler`

*اضغط على **Save** لحفظ التغييرات، قد يستغرق سريان مفعول التحديث من Google عدة دقائق.*

---

## 2. النشر السحابي كخدمة ويب (Render Web Service) 🌐

تطبيق **SmartyAi Order** يتميز ببرمجة كاملة خادمة-عميل (Full-Stack Backend Express + Frontend React) تُدار من خادم Node واحد مدمج.

أثناء تهيئة تطبيق جديد على موقع استضافة **Render**، اتبع الإعدادات المفضلة التالية:

* **نوع التطبيق (Service Type):** اختر **Web Service** (وليس Static Site لضمان تشغيل خادم Node ومحرك الذكاء الاصطناعي وجدولة التتبع).
* **بيئة التشغيل (Runtime):** `Node`
* **أمر البناء (Build Command):**
  ```bash
  npm install && npm run build
  ```
* **أمر التشغيل (Start Command):**
  ```bash
  npm start
  ```

---

## 3. متغيرات البيئة الأساسية (Environment Variables) ⚙️

قم بإضافة المتغيرات السرية التالية في قسم **Environment Variables** بلوحة تحكم الاستضافة السحابية:

| اسم المتغير | الوصف | مثال/الحالة |
| :--- | :--- | :--- |
| `GEMINI_API_KEY` | مفتاح API الخاص بالذكاء الاصطناعي Gemini لاستخراج تفاصيل الطلبات الذكية | ضروري جداً |
| `APP_URL` | رابط النشر الفعلي لبوابة الدفع والمتاجر العامة | `https://your-app.onrender.com` |
| `ENCRYPTION_KEY` | مفتاح عشوائي طويل لتشفير وفك تشفير الـ tokens والبيانات الحساسة | مفتاح سري عشوائي |
| `CHARGILY_SECRET_KEY` | مفتاح Chargily السري الفعال لمعالجة خطط الاشتراكات والمدفوعات | اختياري (للاشتراكات المدفوعة) |
| `YALIDINE_API_ID` | معرّف حساب Yalidine الافتراضي للفرز المباشر والشحن التلقائي للطلبات | اختياري (افتراضي) |
| `YALIDINE_API_TOKEN` | توكن حساب Yalidine الافتراضي للشحن والفرز التلقائي | اختياري (افتراضي) |

---

## 4. مستودع المنتج والمخزون وحركة تتبع تذاكر الشحن الآلية 📦

تم ضبط محرك التطبيق بالخصائص المتقدمة التالية:
1. **جدولة تتبع متكاملة (Automated Tracking Scheduler):** يعمل المجدل الذكي بانتظام كل **5 ساعات** باستمرار للتزامن المباشر مع خوادم Yalidine الحقيقية لتحديث حالات الطلبات، مع وجود معالجة احتياطية (`setInterval`) للتغلب على قيود الخوادم بدون مقاطعة.
2. **إعادة تعبئة المخزون (Inventory Auto-Refill):** عند رجوع أي طرد شحن بنتيجة **"returned"** (مرتجع)، يقوم النظام فوراً بإرجاع وتزويد كمية قطع المنتجات المرتجعة تلقائياً لمستودع التاجر لحماية وحفظ كمية بضائعك دون مجهود يدوي.
3. **منع النقرات المزدوجة (Google SignIn Overlap Guard):** يمنع النظام تداخل جلسات التوثيق أو طلبات الدخول المتكررة عبر الإنترنت مما يوفر حماية أمان كاملة.

---

**فريق SmartyAi Order يتمنى لك تجارة رابحة وتوصيل فائق السهولة!** 🎯🌱
